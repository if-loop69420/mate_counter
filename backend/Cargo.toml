[package]
name = "mate_counter"
version = "0.1.0"
edition = "2024"

[dependencies]
rocket = "0.5.1"
sqlx = { version = "0.8.6", features = ["postgres"] }
tokio = { version = "1.47.1", features = ["full"] }
